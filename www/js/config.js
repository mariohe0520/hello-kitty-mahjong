/**
 * @file config.js
 * @description 全局配置中心 — 游戏参数、规则变体、AI 参数
 * 所有魔法数字统一在此定义，方便调整无需改动业务逻辑
 */

const CONFIG = Object.freeze({

  // ═══════════════════════════════════════════════════════════════
  // 计分
  // ═══════════════════════════════════════════════════════════════
  /** 初始筹码（每人） */
  INITIAL_SCORE: 25000,
  /** 玩家数量 */
  PLAYER_COUNT: 4,
  /** 发牌张数（庄家额外通过正常摸牌获得第14张） */
  DEAL_TILES_EACH: 13,

  // ═══════════════════════════════════════════════════════════════
  // 计时
  // ═══════════════════════════════════════════════════════════════
  /** 玩家操作按钮（吃碰杠胡）自动过牌超时（毫秒） */
  REACTION_TIMEOUT_MS: 20000,
  /** AI 基础思考时间（毫秒） */
  AI_BASE_DELAY_MS: 500,
  /** AI 最短延迟（毫秒，防止太快显得作弊） */
  AI_MIN_DELAY_MS: 180,
  /** 对局开始动画延迟（毫秒） */
  GAME_START_DELAY_MS: 300,

  // ═══════════════════════════════════════════════════════════════
  // 牌河（discard river）显示限制
  // ═══════════════════════════════════════════════════════════════
  /** 下家/上家侧面牌河最多显示张数（超出显示 +N 标记） */
  RIVER_MAX_SIDE: 12,
  /** 玩家自己和对家上下牌河最多显示张数 */
  RIVER_MAX_BOTTOM: 18,

  // ═══════════════════════════════════════════════════════════════
  // 素材路径（可配置）
  // ═══════════════════════════════════════════════════════════════
  /** 牌面 SVG 素材目录 */
  TILE_ASSET_BASE: 'assets/tiles/',
  /** 角色头像目录 */
  CHARACTER_ASSET_BASE: 'assets/characters/',

  // ═══════════════════════════════════════════════════════════════
  // 规则变体参数
  // ═══════════════════════════════════════════════════════════════
  MODES: Object.freeze({
    /** 北京麻将：136张牌，允许吃 */
    beijing: Object.freeze({ deckSize: 136, allowChi: true, label: '北京麻将' }),
    /** 四川血战：108张牌，不允许吃，多赢继续 */
    sichuan: Object.freeze({ deckSize: 108, allowChi: false, label: '四川血战' }),
  }),

  // ═══════════════════════════════════════════════════════════════
  // AI 性格参数（覆盖 characters.js 中未定义的角色）
  // ═══════════════════════════════════════════════════════════════
  AI_PROFILES: Object.freeze({
    cautious:   Object.freeze({ aggression: 0.3, gangRate: 0.6, chiRate: 0.7, bluffRate: 0.05, riskTolerance: 0.3 }),
    balanced:   Object.freeze({ aggression: 0.5, gangRate: 0.7, chiRate: 0.8, bluffRate: 0.10, riskTolerance: 0.5 }),
    aggressive: Object.freeze({ aggression: 0.8, gangRate: 0.9, chiRate: 0.9, bluffRate: 0.15, riskTolerance: 0.8 }),
    friendly:   Object.freeze({ aggression: 0.5, gangRate: 0.7, chiRate: 0.8, bluffRate: 0.05, riskTolerance: 0.5 }),
    defensive:  Object.freeze({ aggression: 0.2, gangRate: 0.4, chiRate: 0.9, bluffRate: 0.02, riskTolerance: 0.2 }),
    tricky:     Object.freeze({ aggression: 0.6, gangRate: 0.6, chiRate: 0.7, bluffRate: 0.35, riskTolerance: 0.6 }),
    wise:       Object.freeze({ aggression: 0.6, gangRate: 0.8, chiRate: 0.6, bluffRate: 0.10, riskTolerance: 0.4 }),
    legendary:  Object.freeze({ aggression: 0.7, gangRate: 0.85, chiRate: 0.75, bluffRate: 0.25, riskTolerance: 0.7 }),
  }),

  // ═══════════════════════════════════════════════════════════════
  // 玩家席位（固定布局）
  // ═══════════════════════════════════════════════════════════════
  SEATS: Object.freeze([
    Object.freeze({ name: '我',   avatar: '🎀', personality: null,        charId: null,    isHuman: true  }),
    Object.freeze({ name: '下家', avatar: '🦊', personality: 'tricky',    charId: 'fox',   isHuman: false }),
    Object.freeze({ name: '对家', avatar: '🐻', personality: 'aggressive', charId: 'bear',  isHuman: false }),
    Object.freeze({ name: '上家', avatar: '🐰', personality: 'defensive', charId: 'bunny', isHuman: false }),
  ]),

  // ═══════════════════════════════════════════════════════════════
  // 风牌顺序
  // ═══════════════════════════════════════════════════════════════
  WIND_SEQUENCE: Object.freeze(['fe', 'fs', 'fw', 'fn']),
  WIND_LABELS: Object.freeze({ fe: '东', fs: '南', fw: '西', fn: '北' }),

  // ═══════════════════════════════════════════════════════════════
  // 存档 key
  // ═══════════════════════════════════════════════════════════════
  STORAGE_PREFIX: 'hk_mj_',
  STORAGE_VERSION: 2,

  // ═══════════════════════════════════════════════════════════════
  // UI
  // ═══════════════════════════════════════════════════════════════
  /** 向听数显示标签 */
  SHANTEN_LABELS: Object.freeze({
    [-1]: '和牌',
    0:    '听牌!',
    1:    '一向听',
    2:    '二向听',
    3:    '三向听',
    4:    '四向听',
    5:    '五向听',
    6:    '六向听',
  }),

});
